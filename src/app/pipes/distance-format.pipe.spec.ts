import { DistanceFormatPipe } from './distance-format.pipe';

describe('DistanceFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
